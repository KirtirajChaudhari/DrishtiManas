# Leave empty when the API is served from the same origin (Docker / Cloud Run / Vercel).
# Set it when the frontend and API are deployed separately, e.g. https://drishti-api-xyz.a.run.app
VITE_API_BASE_URL=
